services:
  db:
    image: postgres:16-alpine
    environment:
      POSTGRES_DB: hortensia
      POSTGRES_USER: hortensia
      POSTGRES_PASSWORD: hortensia
    ports:
      - "5432:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data
volumes:
  postgres_data:
